<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MusicIIgnite" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>
    <style name="CustomToolbarTheme" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="android:actionButtonStyle">@style/BiggerActionButton</item>
    </style>

    <style name="BiggerActionButton" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">50dp</item>
        <item name="android:minHeight">50dp</item>
    </style>
</resources>